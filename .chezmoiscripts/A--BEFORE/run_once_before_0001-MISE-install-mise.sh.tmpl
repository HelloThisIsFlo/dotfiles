#!/bin/bash
# Install mise (version manager) — cross-platform, no Homebrew dependency.
# https://mise.jdx.dev/getting-started.html

MISE_INSTALL_PATH="{{ .mise_bin }}" curl https://mise.jdx.dev/install.sh | sh
