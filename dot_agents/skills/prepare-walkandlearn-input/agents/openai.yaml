interface:
  display_name: "Prepare WalkAndLearn Input (Legacy)"
  short_description: "Prepare clipboard input for legacy LangGraph"
  default_prompt: "Use $prepare-walkandlearn-input to prepare my clipboard conversation for the legacy WalkAndLearn LangGraph UI."
policy:
  allow_implicit_invocation: false
