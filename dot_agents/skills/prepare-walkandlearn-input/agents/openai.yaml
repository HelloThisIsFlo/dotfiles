interface:
  display_name: "Prepare WalkAndLearn Input"
  short_description: "Prepare clipboard input for WalkAndLearn LangGraph"
  default_prompt: "Use $prepare-walkandlearn-input to prepare my clipboard conversation for the WalkAndLearn LangGraph UI."
policy:
  allow_implicit_invocation: false
