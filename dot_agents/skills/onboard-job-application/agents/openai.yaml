interface:
  display_name: "Onboard Job Application"
  short_description: "Evaluate, rank, inspect, and add job applications"
  default_prompt: "Use $onboard-job-application to evaluate one or more roles against my current vault context, inspect their application requirements, and add or update them sequentially in my Obsidian application dashboard."
