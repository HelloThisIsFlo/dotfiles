{{- if eq .chezmoi.os "darwin" -}}
#!/bin/bash

# Brewfile hash: {{ include "dot_Brewfile" | sha256sum }}

brew bundle --no-upgrade || echo "⚠ brew bundle had errors (see above). If cask version mismatches, run: brew upgrade --cask <name>"
{{ end -}}
