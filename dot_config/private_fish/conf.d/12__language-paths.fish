# Placeholder: language binary directories on PATH ($GOPATH/bin, ~/.cargo/bin, ~/.jenv/bin)
# Note: This only adds binaries to PATH => Language env vars (GOPATH, PYTHONSTARTUP, etc.) are configured in 3x TOOL-INTEGRATIONS (33__python.fish, 34__go.fish, etc.)
