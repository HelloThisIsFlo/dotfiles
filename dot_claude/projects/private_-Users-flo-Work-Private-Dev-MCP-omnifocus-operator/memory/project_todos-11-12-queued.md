---
name: Todos 11 and 12 queued for next session
description: Flo created todos 11 (Literal/Annotated for contracts only) and 12 (centralize field descriptions) and wants to be reminded to tackle them when back
type: project
---

Flo created two new todos on 2026-04-01 and wants to tackle them next time he sits down:

- **Todo #11**: Reserve Literal and Annotated types for contract models only — audit `models/` vs `contracts/`, move schema-facing types to contracts, keep core models with plain types.
- **Todo #12**: Centralize field descriptions into constants — follow the `agent_messages/errors.py` / `warnings.py` pattern, replace inline `Field(description=...)` strings.

**Why:** Both emerged from Phase 36.2 schema sweep work. He explicitly asked to be reminded.

**How to apply:** At the start of his next session, remind him these two are queued up and ask which one he wants to start with (or if priorities changed).
